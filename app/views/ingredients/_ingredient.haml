.col-md-4.ingredient
  .name.text-center
    %h2= ingredient.name

  = link_to image_tag(ingredient.image.url, class: 'center adaptive-image'), ingredient_path(ingredient.slug)

  - if current_user&.admin?
    .actions.center
      = link_to 'Google images', ingredient_images_path(ingredient_id: ingredient.slug), class: 'btn btn-primary center'

      = link_to 'Delete', ingredient_path(ingredient.slug), method: :delete, class: 'btn btn-danger center'