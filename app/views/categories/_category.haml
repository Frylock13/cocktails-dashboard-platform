.row
  .col-md-3
    .name
      %h1= link_to category.name, category_path(category.slug)

    - if current_user&.admin?
      .actions.center
        = link_to 'Edit', edit_category_path(category.slug), class: 'btn btn-warning center'